# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'P10_Reciplease' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for P10_Reciplease
  pod 'Alamofire'
  pod 'AlamofireImage'
  pod 'SwiftyJSON'
  pod 'Cosmos', '~> 15.0'

  target 'P10_RecipleaseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'P10_RecipleaseUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
